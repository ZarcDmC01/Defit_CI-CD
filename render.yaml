services:
  - type: web
    name: defit-app
    runtime: docker
    dockerfilePath: ./Dockerfile.api
    dockerContext: .
    branch: main
    autoDeploy: true
    envVars:
      - key: INFERENCE_ENGINE
        value: mock
